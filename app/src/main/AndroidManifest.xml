<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phuphuc.chonhinhgiongnhau">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_doremon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_doremon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChọnHìnhGiốngNhau">
        <activity android:name=".ImageActivity" android:theme="@android:style/Theme.Holo.Dialog"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>